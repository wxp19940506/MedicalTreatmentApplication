<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_my_holiday"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="iok.la.com.medicaltreatmentapplication.activities.MyHolidayActivity">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@color/titlebar_background"
        android:gravity="center">
        <ImageButton
            android:id="@+id/turn_last"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:padding="10dp"
            android:background="@android:color/transparent"
            android:src="@mipmap/zuob"
            />
        <TextView
            android:id="@+id/title"
            style="@style/Title_Style"
            android:text="@string/my_holiday"/>
        <ImageButton
            android:id="@+id/add_holiday"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:src="@mipmap/plus"
            android:layout_alignParentRight="true"
            android:padding="15dp"
            android:background="@android:color/transparent"/>
    </RelativeLayout>
    <iok.la.com.medicaltreatmentapplication.widget.RefreshExpandableListView
        android:id="@+id/myholidays"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@null"
        android:groupIndicator="@null">
    </iok.la.com.medicaltreatmentapplication.widget.RefreshExpandableListView>
</LinearLayout>
